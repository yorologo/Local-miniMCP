# Technical reference

These documents preserve deeper protocol, lifecycle, integration and historical implementation detail useful to maintainers.

They are **not** the primary source for user installation/operation commands when a CURRENT document in `docs/` provides a newer contract.

If a reference document conflicts with CURRENT behavior, fix or annotate the reference; do not make production conform to obsolete text.
