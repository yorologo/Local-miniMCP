# Línea Base Técnica - Raspberry Pi Gateway (2026-09-08)

Documento de inventario técnico y diagnóstico exhaustivo del dispositivo objetivo para el proyecto **MCP Raspberry Pi Gateway**.

---

## 1. Identificación y Hardware

| Parámetro | Valor Verificado |
|---|---|
| **Modelo** | Raspberry Pi Model A Plus Rev 1.1 |
| **SoC** | Broadcom BCM2835 |
| **Revisión de Placa** | 0015 |
| **Número de Serie** | `0000000069bd3834` |
| **Procesador (CPU)** | ARMv6-compatible processor rev 7 (v6l) (1 núcleo @ 700 MHz, BogoMIPS: 697.95) |
| **Arquitectura** | `armv6l` (32-bit) |
| **Características CPU** | `half thumb fastmult vfp edsp java tls` |
| **Memoria RAM** | 176 MiB total utilizable en SO (compartida con GPU VideoCore de 256 MB físicos) |
| **Memoria Disponible** | ~94 MiB disponible en reposo (~32 MiB en uso, ~80 MiB libre, ~62 MiB buffers/cache) |
| **Swap** | Archivo `/var/swap` de 100 MiB (102396 KiB, prioridad -2) |

---

## 2. Almacenamiento y Filesystem

Salida verificada mediante `df -h`, `lsblk` y `lsblk -f`:

| Dispositivo | FSTYPE | Etiqueta | UUID / PFI | Tamaño | Usado | Disponible | Uso % | Punto de Montaje |
|---|---|---|---|---|---|---|---|---|
| `mmcblk0` | - | - | - | 14.7G | - | - | - | Tarjeta MicroSD |
| `mmcblk0p1` | vfat | `bootfs` | `CEE7-0DF0` | 255M | 51M | 205M | 20% | `/boot` |
| `mmcblk0p2` | ext4 | `rootfs` | `ebadab55-bd70-46a9-8f18-995c136fb5b2` | 14.5G | 2.1G | 12.0G | 16% | `/` |

---

## 3. Sistema Operativo y Kernel

| Componente | Detalle Verificado |
|---|---|
| **Distribución** | Raspbian GNU/Linux 11 (bullseye) |
| **Versión ID** | `11` |
| **Codename** | `bullseye` |
| **ID Like** | `debian` |
| **Kernel** | `Linux MCP-Pi 6.1.21+ #1642 Mon Apr 3 17:19:14 BST 2023 armv6l GNU/Linux` |
| **Hostname del sistema** | `MCP-Pi` (actualizado desde `YorPi` en `/etc/hostname` y `/etc/hosts`) |

---

## 4. Red e Interfaces

### 4.1 Interfaz Activa y Enrutamiento
- **Ruta por defecto**: `default via 192.168.68.1 dev wlan0 proto dhcp src 192.168.68.85 metric 202`
- **Subred LAN**: `192.168.68.0/22` (Broadcast: `192.168.71.255`)
- **Interfaz activa**: `wlan0`
- **Dirección IPv4**: `192.168.68.85`
- **Dirección IPv6 Link-Local**: `fe80::a7b2:2a4a:7c51:7192/64`
- **Dirección MAC física**: `8c:90:2d:ac:e5:c0` (confirmada en `/sys/class/net/wlan0/address`)
- **Interfaz Loopback (`lo`)**: `127.0.0.1/8`, `::1/128`, MAC `00:00:00:00:00:00`

### 4.2 Adaptador Wi-Fi USB y Driver
- **Dispositivo USB**: `Bus 001 Device 002: ID 0bda:8179 Realtek Semiconductor Corp. RTL8188EUS 802.11n Wireless Network Adapter`
- **Controlador/Driver**: `r8188eu` (`/sys/bus/usb/drivers/r8188eu`)
- **Métricas inalámbricas**: ESSID `MUTINIFNI`, Frecuencia 2.422 GHz, Link Quality 100/100, Nivel de señal -32 dBm, Bitrate 150 Mb/s.

### 4.3 Resolución DNS (`/etc/resolv.conf`)
- `nameserver 192.168.68.54` (Servidor Pi-hole local primario)
- `nameserver 94.140.14.14` (AdGuard DNS público secundario)

### 4.4 Pruebas de Conectividad (Ping ICMP)
- **Gateway LAN (`192.168.68.1`)**: 4 paquetes transmitidos, 4 recibidos (0% pérdida, RTT avg: 9.25 ms) -> **PASS**
- **Pi-hole (`192.168.68.54`)**: 4 paquetes transmitidos, 4 recibidos (0% pérdida, RTT avg: 37.29 ms) -> **PASS**
- **Internet IP (`1.1.1.1`)**: 4 paquetes transmitidos, 4 recibidos (0% pérdida, RTT avg: 33.49 ms) -> **PASS**
- **DNS / Dominio (`raspberrypi.com`)**: Resuelto a `172.67.154.53`, 4 paquetes transmitidos, 4 recibidos (0% pérdida, RTT avg: 50.96 ms) -> **PASS**

---

## 5. Software Base, Servicios y Privilegios

### 5.1 SSH
- **Versión**: `OpenSSH_8.4p1 Raspbian-5+deb11u5, OpenSSL 1.1.1w 11 Sep 2023`
- **Estado de servicio**: `active (running)`
- **Puerto**: 22

### 5.2 Python
- **Versión**: `Python 3.9.2` (nativo en Raspbian Bullseye)

### 5.3 Usuario y Sudo
- **Usuario**: `Yorologo` (uid=1000, gid=1000)
- **Grupos**: `adm`, `dialout`, `cdrom`, `sudo`, `audio`, `video`, `plugdev`, `games`, `users`, `input`, `render`, `netdev`, `gpio`, `i2c`, `spi`
- **Privilegios Sudo (`sudo -l`)**:
  - `(ALL : ALL) ALL`
  - `(ALL) NOPASSWD: ALL`
- **SUDO_AVAILABLE**: `YES`

### 5.4 Estado de systemd y Unidades Fallidas
- `systemctl --failed`: `0 loaded units listed` (Ninguna unidad fallida).

---

## 6. Advertencias y Observaciones de Logs

### 6.1 `journalctl -p warning -b` (49 eventos)
1. **Módulos de kernel en staging**:
   - `r8188eu` (driver Wi-Fi Realtek): advertencia de módulo procedente del directorio `staging`.
   - `snd_bcm2835`, `bcm2835_codec`, `bcm2835_v4l2`, `vc_sm_cma`: controladores multimedia de Broadcom en staging.
2. **Base de datos regulatoria Wi-Fi**:
   - `crda failed with exit code 249` y `loaded regulatory.db is malformed or signature is missing/invalid`. No impide el enlace Wi-Fi actual.
3. **`wpa_supplicant`**:
   - Mensajes aislados de `ioctl[SIOCSIWAP]: Operation not permitted` al negociar el adaptador USB.

### 6.2 Advertencia Post-Quantum en SSH
- El cliente SSH moderno emite advertencia de que la conexión actual con OpenSSH 8.4p1 no utiliza intercambio de claves post-cuántico.

---

## 7. Conflicto de Identidad y Hostname (RESUELTO)

- **Hostname anterior del gateway**: `YorPi`
- **Hostname actual del gateway**: `MCP-Pi` (`192.168.68.85`, MAC `8c:90:2d:ac:e5:c0`)
- **Hostname de Pi-hole**: `YorPi` (`192.168.68.54`, preservado sin modificaciones)
- **Estado de colisión**: **NO (RESUELTA)** en Fase 2A.
- **Configuración aplicada**:
  - `sudo hostnamectl set-hostname MCP-Pi`
  - `/etc/hosts` actualizado asociando `127.0.1.1` con `MCP-Pi`.
  - Reinicio del sistema completado con validación de conectividad.

---

## 8. Diagnóstico de APT y Evaluación UPDATE_GATE

### 8.1 Repositorios Configurados
- `/etc/apt/sources.list`:
  `deb http://raspbian.raspberrypi.org/raspbian/ bullseye main contrib non-free rpi`
- `/etc/apt/sources.list.d/raspi.list`:
  `deb http://archive.raspberrypi.org/debian/ bullseye main`

### 8.2 Diagnóstico de Causa Raíz (Timeout Inicial)
- En el intento inicial de Fase 1, la descarga de `Packages.gz` (18.3 MB) desde `mirrordirector.raspbian.org` (93.93.128.193) agotó el tiempo de espera por descarga lenta sobre Wi-Fi (`Connection timed out [IP: 93.93.128.193 80]`).
- Diagnóstico adicional comprobó:
  - DNS operativo hacia los espejos.
  - Reloj del sistema sincronizado por NTP (`America/Mexico_City`, UTC sincronizado).
  - Almacenamiento holgado (12 GB libres en `/`).
  - Memoria RAM y swap estables (~92 MB disponibles, swap prácticamente libre).
  - Espejos respondiendo HTTP 200 OK.

### 8.3 Reintento de `sudo apt update` (Fase 2A)
- Resultado: **EXITOSO**.
- Salida: `Fetched 2,191 kB in 1min 23s (26.5 kB/s)`.
- Códigos de retorno: 0. Árbol de dependencias y estado leídos correctamente.

### 8.4 Paquetes Actualizables y Retenidos
- `apt list --upgradable`: **58 paquetes actualizables**.
- Naturaleza de paquetes: Parches de seguridad y estabilidad de librerías base (`openssh-server`, `openssh-client`, `openssl`, `libssl1.1`, `sudo`, `systemd`, `udev`, `python3.9`, etc.).
- Paquetes de kernel: **NINGUNO** (`raspberrypi-kernel` no se encuentra en la lista de actualización).
- `apt-mark showhold`: 0 paquetes retenidos.

### 8.5 Driver Wi-Fi y Estabilidad de Red
- Driver en uso: `r8188eu` (sin modificaciones).
- Comportamiento post-reboot: enlace Wi-Fi reestablecido en ~50-60 segundos tras arranque, link quality 100/100, señal -32 dBm, ping 0% pérdidas a gateway LAN, Pi-hole e Internet.

### 8.6 Veredicto UPDATE_GATE
```text
UPDATE_GATE: PASS
```
**Criterio:** Los índices APT son 100% íntegros, la red y SSH son estables tras reboot, los 58 paquetes actualizables son librerías del sistema sin impacto en kernel ni módulos de red, y el almacenamiento dispone de más de 12 GB libres.


